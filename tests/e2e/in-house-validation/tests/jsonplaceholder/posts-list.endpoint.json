{
  "id": "jsonplaceholder.posts_list",
  "name": "GET /posts — list with pagination assertion",
  "method": "GET",
  "url": "${env.base_url}/posts",
  "tags": ["jsonplaceholder", "read", "pagination"],
  "markers": ["regression"],
  "prod_safe": true,
  "request": {},
  "response": {
    "expected_status": 200,
    "sla_ms": 5000,
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "userId", "title", "body"],
        "properties": {
          "id": { "type": "integer" },
          "userId": { "type": "integer" },
          "title": { "type": "string" },
          "body": { "type": "string" }
        }
      }
    }
  }
}

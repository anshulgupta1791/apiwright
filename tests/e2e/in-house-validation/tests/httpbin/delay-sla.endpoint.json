{
  "id": "httpbin.delay_1s",
  "name": "GET /delay/1 — SLA budget validation",
  "method": "GET",
  "url": "${env.base_url}/delay/1",
  "tags": ["httpbin", "read", "sla"],
  "markers": ["regression"],
  "prod_safe": true,
  "request": {},
  "response": {
    "expected_status": 200,
    "sla_ms": 5000,
    "schema": {
      "type": "object",
      "required": ["url"],
      "properties": {
        "url": { "type": "string" }
      }
    }
  }
}
